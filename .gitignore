# Python
.venv/
__pycache__/
# generated index (index.npz + chunks.jsonl)
backend/storage/
# eval run records (regenerable, machine-specific); the accepted rates live
# in each dataset's committed baseline.json
evals/results/

# Node (frontend)
node_modules/
dist/
*.log
# local env overrides (.env.local etc.) — never committed
*.local

# OS / editor
.DS_Store
.idea/
.vscode/
